from collections import defaultdict

not_a_valid_push_token_message = defaultdict(
    lambda: "Sent push token is not a valid token.",
    {
        "fr": "Le push token envoyé n'est pas valide.",
    }
)
